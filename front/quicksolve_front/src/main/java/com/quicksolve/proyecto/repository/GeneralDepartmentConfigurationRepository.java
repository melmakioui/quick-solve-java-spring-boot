package com.quicksolve.proyecto.repository;

import com.quicksolve.proyecto.entity.GeneralDepartmentConfiguration;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GeneralDepartmentConfigurationRepository extends JpaRepository<GeneralDepartmentConfiguration, Long> {
}
