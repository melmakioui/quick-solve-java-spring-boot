spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://localhost:3306/esquelet?useSSL=false&serverTimeZone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=
spring.datasource.password=
upload= #where the resorucehandler will look for save the files
upload.path= #where the files will be uploaded in the server
spring.servlet.multipart.max-file-size=10MB
spring.messages.basename=lang/res
#Mailgun
API_KEY=
mailgun.from=
mailgun.domain=

#For insert data in the database at the start of the application
#spring.jpa.defer-datasource-initialization=true
#spring.sql.init.mode=always
#spring.sql.init.data-locations=classpath:data.sql -> this file has to be in the resources folder
